// 1. Find the button using its HTML id
const button = document.getElementById('order-btn');

// 2. Make it do something when clicked
button.addEventListener('click', function() {
    alert("Order placed!");
});